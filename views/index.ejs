<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Cloud Land | Register</title>
    <link rel="stylesheet" href="/style.css" />
  </head>
  <body>
    <header>
      <h1>CLOUD LAND</h1>
      <p>Login or register below so you can upload to the cloud.</p>
    </header>
    <% if (errors) { %>
      <%- include('partials/errors', { errors: errors }) %>
    <% } %>
    <div class="index-forms-container">
    <%- include('partials/loginForm') %>
    <%- include('partials/registerForm', {prefilledValues: sentValues}) %>
    </div>
  </body>
</html>
