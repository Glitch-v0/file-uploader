<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cloud | <%= file.name %> </title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <%- include('partials/navbar', {links: ['cloud', 'search', 'logout']}) %>
    <% if (errors) { %>
        <%- include('partials/errors', { errors: errors }) %>
    <% } %>

    <h1>File Details</h1>
    <h2>Name: <%= file.name %></h2>
    <h2>Type: <%= file.type %></h2>
    <h2>Size: <%= file.size %> bytes</h2>
    <h1>
        <a href= <%= `/sharedFile/${file.id}/download` %> class="download" >Download</a>
    </h1>
</body>
</html>